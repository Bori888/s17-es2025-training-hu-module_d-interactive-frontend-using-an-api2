version: "3.9"

services:
  frontend:
    image: node:20-alpine
    container_name: skillshare-frontend
    working_dir: /app
    volumes:
      - ./frontend:/app
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
